spring.application.name=e_commerce

jwt.secret=your-256-bit-secret-key-here-make-it-at-least-32-bytes-long

# Cấu hình kết nối database MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/e_commerce?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=trung2205

# Driver MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configurations
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Tùy chọn cấu hình connection pool (HikariCP - mặc định)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
